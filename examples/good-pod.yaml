apiVersion: v1
kind: Pod
metadata:
  name: good-pod
  labels:
    app: demo
    owner: platform-team
spec:
  containers:
    - name: web
      image: nginx:1.25.3
      resources:
        limits:
          cpu: 100m
          memory: 128Mi
      securityContext:
        allowPrivilegeEscalation: false
